package euler.prob001_100.prob080

import euler.traits.UtilResult

/**
  * Created by Ricardo
  */
object Prob080 extends UtilResult {
  override def calc: Long = {
//    val precision = new MathContext(102)
//
//    val v = for {
//      n <- 1 to 100
//      big = spire.math.Algebraic(n).nroot(2).toBigDecimal(precision)
//      if big.toString.count(_ == '0') < 100
//      bigSum = big.toString.take(101).filterNot(_ == '.').map(_.toInt - '0').sum
//    } yield (big, bigSum)
//
//    result(v.unzip._2.sum)

    ???
  }
}
